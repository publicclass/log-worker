{
  "name": "log-worker",
  "version": "0.0.0",
  "description": "A logger which uses the FileSystem API in a WebWorker. For multi-channeled logs.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/publicclass/log-worker.git"
  },
  "author": "Robert Sköld <robert@publicclass.se>",
  "license": "MIT",
  "devDependencies": {
    "uglify-js": "~2.2.4"
  }
}
